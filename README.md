# Note Nest

A small DOM-based notes manager built with Django, HTML, CSS, and JavaScript.

## Run locally

```bash
python3 -m pip install -r requirements.txt
python3 manage.py runserver
```

Then open http://127.0.0.1:8000/.

Notes are managed in the browser with JavaScript and saved in `localStorage`. Django provides the project and page route.
